# hurryman2212's composed tree: openwrt/openwrt main + OpenWRT-fanboy `offload`
# patch-unique commits + the `ubi2-oc` overclock delta + personal fixes.
# Public branch; the token URL in the upstream profile is push credentials, not read.
REPO_URL="https://github.com/hurryman2212/OpenW1700k-test"
REPO_BRANCH="offload-oc"
