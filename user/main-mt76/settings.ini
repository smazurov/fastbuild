# Mainline OpenWrt, not the OpenW1700k fork.
# Chosen so the build does NOT carry 722e903c5b "mac80211: emit switchdev FDB DEL on
# STA disconnect", which emits `mt7530 ... failed to delete <sta> vid 0 from fdb: -2`
# on every station disconnect once MLO is enabled.
REPO_URL="https://github.com/openwrt/openwrt"
REPO_BRANCH="main"
